---
description: "Root hub for this research vault. Lists active domains and links to their topic maps."
type: index
---

# {VAULT_NAME}

A second-brain research vault for **{DOMAIN}**.

## Domains

(Domains will appear here as you create domain MOCs. Start with one — `/init` may have created a placeholder. Rename it.)

- [[_<your-first-domain>]] — what this domain covers

## Recent claims

(Updated by `/anneal --scope=vault` or manually. The model surfaces recent work on session start by reading `goals.md` + walking notes/claims/ by mtime.)

## How to use this vault

- Drop a source in `inbox/` and run `/learn <path>`.
- Or write a single claim directly in `notes/claims/` and `/connect` it.
- See `CLAUDE.md` for the full verb set and atom contract.
